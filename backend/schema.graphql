type Query {
    hello: String
    list: [String]
}

type Mutation {
    addUser(email: String!, name: String, lName: String, color: String): User!
}

type User {
    id: String!
    email: String!
    name: String
    lName: String
    configuration: UserConfig!
}

type UserConfig {
    color: String
}
